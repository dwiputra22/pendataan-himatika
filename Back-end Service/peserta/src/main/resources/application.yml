server:
  error:
    include-message: always
    include-binding-errors: always
  port: 8082

spring:
  application:
    name: pendataan-peserta
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/himatika
    username: root
    password:
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect
        use_sql_comments: true
    show-sql: false

  mail:
    host: smtp.gmail.com
    port: 587
    username: dwiputraramadhan221101@gmail.com
    password: ggaibsliwcjjxvbh
    protocol: smtp
    tls: true
    properties.mail.smtp:
      auth: true
      starttls.enable: true
      ssl.trust: smtp.gmail.com
